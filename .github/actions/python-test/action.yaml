name: Basic Python tests
description: This runs most Python tests except for intensive property-based tests.
inputs:
  python-version:
    description: "Python version to test"
    required: false
    default: "3.8"
  test-globs:
    description: "Globs to use for testing"
    required: true
runs:
  using: "composite"
  steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Set up Python ${{ inputs.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ inputs.python-version }}
    - name: Install test dependencies
      run: python -m pip install -r requirements.txt && python -m pip install -r requirements_dev.txt
      shell: bash
    - name: Upgrade SQLite
      run: python bin/windows_upgrade_sqlite.py
      shell: bash
      if: runner.os == 'Windows'
    - name: Test
      run: python -m pytest ${{ inputs.test-globs }}
      shell: bash
