name: (temp) test pypi publish
on:
  pull_request:

jobs:
  publish:
    uses: ./.github/workflows/_build_release_pypi.yml
    secrets: inherit
    with:
      publish_to_test_pypi: true
      version: "0.6.4.dev233"
