[toolchain]
# Keep this version in sync with:
# - rust/cli/Dockerfile
# - rust/load/Dockerfile
# - rust/log/Dockerfile
# - rust/worker/Dockerfile
channel = "1.81.0"
components = ["rustfmt", "clippy"]
