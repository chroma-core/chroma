{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Fastembed Text Embedding Function Schema",
    "description": "Schema for the Fastembed text embedding function configuration",
    "version": "1.0.0",
    "type": "object",
    "properties": {
        "model_name": {
            "type": "string",
            "description": "Identifier of the Fastembed text embedding model"
        },
        "task": {
            "type": [
                "string",
                "null"
            ],
            "enum": [
                "document",
                "query",
                "passage",
                null
            ],
            "description": "Task to perform, can be 'document', 'query', or 'passage'"
        },
        "query_config": {
            "type": [
                "object",
                "null"
            ],
            "description": "Configuration for query embedding",
            "properties": {
                "task": {
                    "type": "string",
                    "enum": [
                        "document",
                        "query",
                        "passage"
                    ],
                    "description": "Task to perform for query embedding"
                }
            },
            "additionalProperties": false
        },
        "cache_dir": {
            "type": [
                "string",
                "null"
            ],
            "description": "The path to the cache directory"
        },
        "threads": {
            "type": [
                "integer",
                "null"
            ],
            "description": "The number of threads to use for the model"
        },
        "providers": {
            "type": [
                "array",
                "null"
            ],
            "description": "ONNX Runtime providers to use for the model"
        },
        "cuda": {
            "type": "boolean",
            "description": "Whether to use CUDA"
        },
        "device_ids": {
            "type": [
                "array",
                "null"
            ],
            "description": "The device IDs to use for the model",
            "items": {
                "type": "integer"
            }
        },
        "lazy_load": {
            "type": "boolean",
            "description": "Whether to lazy load the model"
        },
        "kwargs": {
            "type": "object",
            "description": "Additional arguments to pass to the model",
            "additionalProperties": {
                "type": [
                    "string",
                    "integer",
                    "number",
                    "boolean",
                    "array",
                    "object"
                ]
            }
        }
    },
    "required": [
        "model_name"
    ],
    "additionalProperties": false
}
