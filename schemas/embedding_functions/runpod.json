{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RunPod Embedding Function Schema",
    "description": "Schema for the RunPod embedding function configuration",
    "version": "1.0.0",
    "type": "object",
    "properties": {
        "model_name": {
            "type": "string",
            "description": "The name of the model to use for text embeddings"
        },
        "endpoint_id": {
            "type": ["string", "null"],
            "description": "The endpoint ID for the RunPod API"
        },
        "api_key_env_var": {
            "type": "string",
            "description": "Environment variable name that contains your API key for the RunPod API"
        },
        "timeout": {
            "type": ["integer", "null"],
            "description": "Timeout in seconds for API requests"
        }
    },
    "required": ["api_key_env_var", "endpoint_id", "model_name"],
    "additionalProperties": false
}
