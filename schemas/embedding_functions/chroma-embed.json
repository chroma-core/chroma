{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Chroma Embedding Function Schema",
    "description": "Schema for the Chroma embedding function configuration",
    "version": "1.0.0",
    "type": "object",
    "properties": {
        "model_id": {
            "type": "string",
            "description": "The ID of the model to use for embeddings"
        },
        "api_key_env_var": {
            "type": "string",
            "description": "Environment variable name that contains your API key for the Chroma Embedding API"
        },
        "task": {
            "type": [
                "string",
                "null"
            ],
            "description": "The task for which embeddings are being generated. Defaults to \"code\"."
        }
    },
    "required": [
        "api_key_env_var",
        "model_id"
    ],
    "additionalProperties": false
}