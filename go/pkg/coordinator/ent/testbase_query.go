// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/chroma-core/chroma/go/pkg/coordinator/ent/predicate"
	"github.com/chroma-core/chroma/go/pkg/coordinator/ent/testbase"
)

// TestBaseQuery is the builder for querying TestBase entities.
type TestBaseQuery struct {
	config
	ctx        *QueryContext
	order      []testbase.OrderOption
	inters     []Interceptor
	predicates []predicate.TestBase
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TestBaseQuery builder.
func (tbq *TestBaseQuery) Where(ps ...predicate.TestBase) *TestBaseQuery {
	tbq.predicates = append(tbq.predicates, ps...)
	return tbq
}

// Limit the number of records to be returned by this query.
func (tbq *TestBaseQuery) Limit(limit int) *TestBaseQuery {
	tbq.ctx.Limit = &limit
	return tbq
}

// Offset to start from.
func (tbq *TestBaseQuery) Offset(offset int) *TestBaseQuery {
	tbq.ctx.Offset = &offset
	return tbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tbq *TestBaseQuery) Unique(unique bool) *TestBaseQuery {
	tbq.ctx.Unique = &unique
	return tbq
}

// Order specifies how the records should be ordered.
func (tbq *TestBaseQuery) Order(o ...testbase.OrderOption) *TestBaseQuery {
	tbq.order = append(tbq.order, o...)
	return tbq
}

// First returns the first TestBase entity from the query.
// Returns a *NotFoundError when no TestBase was found.
func (tbq *TestBaseQuery) First(ctx context.Context) (*TestBase, error) {
	nodes, err := tbq.Limit(1).All(setContextOp(ctx, tbq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{testbase.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tbq *TestBaseQuery) FirstX(ctx context.Context) *TestBase {
	node, err := tbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TestBase ID from the query.
// Returns a *NotFoundError when no TestBase ID was found.
func (tbq *TestBaseQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tbq.Limit(1).IDs(setContextOp(ctx, tbq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{testbase.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tbq *TestBaseQuery) FirstIDX(ctx context.Context) int {
	id, err := tbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TestBase entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TestBase entity is found.
// Returns a *NotFoundError when no TestBase entities are found.
func (tbq *TestBaseQuery) Only(ctx context.Context) (*TestBase, error) {
	nodes, err := tbq.Limit(2).All(setContextOp(ctx, tbq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{testbase.Label}
	default:
		return nil, &NotSingularError{testbase.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tbq *TestBaseQuery) OnlyX(ctx context.Context) *TestBase {
	node, err := tbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TestBase ID in the query.
// Returns a *NotSingularError when more than one TestBase ID is found.
// Returns a *NotFoundError when no entities are found.
func (tbq *TestBaseQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tbq.Limit(2).IDs(setContextOp(ctx, tbq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{testbase.Label}
	default:
		err = &NotSingularError{testbase.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tbq *TestBaseQuery) OnlyIDX(ctx context.Context) int {
	id, err := tbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TestBases.
func (tbq *TestBaseQuery) All(ctx context.Context) ([]*TestBase, error) {
	ctx = setContextOp(ctx, tbq.ctx, "All")
	if err := tbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TestBase, *TestBaseQuery]()
	return withInterceptors[[]*TestBase](ctx, tbq, qr, tbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tbq *TestBaseQuery) AllX(ctx context.Context) []*TestBase {
	nodes, err := tbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TestBase IDs.
func (tbq *TestBaseQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tbq.ctx.Unique == nil && tbq.path != nil {
		tbq.Unique(true)
	}
	ctx = setContextOp(ctx, tbq.ctx, "IDs")
	if err = tbq.Select(testbase.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tbq *TestBaseQuery) IDsX(ctx context.Context) []int {
	ids, err := tbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tbq *TestBaseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tbq.ctx, "Count")
	if err := tbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tbq, querierCount[*TestBaseQuery](), tbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tbq *TestBaseQuery) CountX(ctx context.Context) int {
	count, err := tbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tbq *TestBaseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tbq.ctx, "Exist")
	switch _, err := tbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tbq *TestBaseQuery) ExistX(ctx context.Context) bool {
	exist, err := tbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TestBaseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tbq *TestBaseQuery) Clone() *TestBaseQuery {
	if tbq == nil {
		return nil
	}
	return &TestBaseQuery{
		config:     tbq.config,
		ctx:        tbq.ctx.Clone(),
		order:      append([]testbase.OrderOption{}, tbq.order...),
		inters:     append([]Interceptor{}, tbq.inters...),
		predicates: append([]predicate.TestBase{}, tbq.predicates...),
		// clone intermediate query.
		sql:  tbq.sql.Clone(),
		path: tbq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (tbq *TestBaseQuery) GroupBy(field string, fields ...string) *TestBaseGroupBy {
	tbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TestBaseGroupBy{build: tbq}
	grbuild.flds = &tbq.ctx.Fields
	grbuild.label = testbase.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (tbq *TestBaseQuery) Select(fields ...string) *TestBaseSelect {
	tbq.ctx.Fields = append(tbq.ctx.Fields, fields...)
	sbuild := &TestBaseSelect{TestBaseQuery: tbq}
	sbuild.label = testbase.Label
	sbuild.flds, sbuild.scan = &tbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TestBaseSelect configured with the given aggregations.
func (tbq *TestBaseQuery) Aggregate(fns ...AggregateFunc) *TestBaseSelect {
	return tbq.Select().Aggregate(fns...)
}

func (tbq *TestBaseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tbq); err != nil {
				return err
			}
		}
	}
	for _, f := range tbq.ctx.Fields {
		if !testbase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tbq.path != nil {
		prev, err := tbq.path(ctx)
		if err != nil {
			return err
		}
		tbq.sql = prev
	}
	return nil
}

func (tbq *TestBaseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TestBase, error) {
	var (
		nodes = []*TestBase{}
		_spec = tbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TestBase).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TestBase{config: tbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tbq *TestBaseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tbq.querySpec()
	_spec.Node.Columns = tbq.ctx.Fields
	if len(tbq.ctx.Fields) > 0 {
		_spec.Unique = tbq.ctx.Unique != nil && *tbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tbq.driver, _spec)
}

func (tbq *TestBaseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(testbase.Table, testbase.Columns, sqlgraph.NewFieldSpec(testbase.FieldID, field.TypeInt))
	_spec.From = tbq.sql
	if unique := tbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tbq.path != nil {
		_spec.Unique = true
	}
	if fields := tbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, testbase.FieldID)
		for i := range fields {
			if fields[i] != testbase.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tbq *TestBaseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tbq.driver.Dialect())
	t1 := builder.Table(testbase.Table)
	columns := tbq.ctx.Fields
	if len(columns) == 0 {
		columns = testbase.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tbq.sql != nil {
		selector = tbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tbq.ctx.Unique != nil && *tbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tbq.predicates {
		p(selector)
	}
	for _, p := range tbq.order {
		p(selector)
	}
	if offset := tbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TestBaseGroupBy is the group-by builder for TestBase entities.
type TestBaseGroupBy struct {
	selector
	build *TestBaseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tbgb *TestBaseGroupBy) Aggregate(fns ...AggregateFunc) *TestBaseGroupBy {
	tbgb.fns = append(tbgb.fns, fns...)
	return tbgb
}

// Scan applies the selector query and scans the result into the given value.
func (tbgb *TestBaseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tbgb.build.ctx, "GroupBy")
	if err := tbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestBaseQuery, *TestBaseGroupBy](ctx, tbgb.build, tbgb, tbgb.build.inters, v)
}

func (tbgb *TestBaseGroupBy) sqlScan(ctx context.Context, root *TestBaseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tbgb.fns))
	for _, fn := range tbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tbgb.flds)+len(tbgb.fns))
		for _, f := range *tbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TestBaseSelect is the builder for selecting fields of TestBase entities.
type TestBaseSelect struct {
	*TestBaseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tbs *TestBaseSelect) Aggregate(fns ...AggregateFunc) *TestBaseSelect {
	tbs.fns = append(tbs.fns, fns...)
	return tbs
}

// Scan applies the selector query and scans the result into the given value.
func (tbs *TestBaseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tbs.ctx, "Select")
	if err := tbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestBaseQuery, *TestBaseSelect](ctx, tbs.TestBaseQuery, tbs, tbs.inters, v)
}

func (tbs *TestBaseSelect) sqlScan(ctx context.Context, root *TestBaseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tbs.fns))
	for _, fn := range tbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
