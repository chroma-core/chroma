[tox]
envlist = py{38,39,310}
minversion = 3.3.0
isolated_build = true

[testenv]
deps =
    check-manifest >= 0.42
    pytest
commands =
    check-manifest --ignore 'tox.ini,DEVELOP.md,tests/**'
    py.test tests {posargs}
