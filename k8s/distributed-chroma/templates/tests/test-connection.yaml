apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "distributed-chroma.fullname" . }}-test-connection"
  labels:
    {{- include "distributed-chroma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "distributed-chroma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
