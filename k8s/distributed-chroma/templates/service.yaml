apiVersion: v1
kind: Service
metadata:
  name: {{ include "distributed-chroma.fullname" . }}
  labels:
    {{- include "distributed-chroma.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distributed-chroma.selectorLabels" . | nindent 4 }}
