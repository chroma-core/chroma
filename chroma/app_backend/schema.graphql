# schema driven dev with ariadne

schema {
    query: Query
    mutation: Mutation
}

type DataPointsResult {
    data: String
}

type Query {
    datapoints: DataPointsResult!
}

# had to have something in here to not throw an error
type Mutation {
    markDone(pointID: String!): DataPointsResult!
}